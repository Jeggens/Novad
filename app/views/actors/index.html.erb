<h1>Actors
    <%= link_to "Add a new Actor", new_actor_path, class: "btn btn-primary btn-lg", style: "float: right" %></h1>

<% @actors.each do |actor| %>
<p><%= link_to actor.name, actor %></p>
<% end %>
